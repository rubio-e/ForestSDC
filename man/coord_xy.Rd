% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coord_xy.R
\name{coord_xy}
\alias{coord_xy}
\title{Convert azimuth and distance to XY coordinates}
\usage{
coord_xy(azi, dis, r = NULL, center = NULL, deg = TRUE)
}
\arguments{
\item{azi}{Numeric vector of azimuth angles.}

\item{dis}{Numeric vector of distances, same length as \code{azi}.}

\item{r}{Optional numeric scalar for centering. If provided, coordinates will be shifted by \code{r/2}.}

\item{center}{Optional numeric vector of length 2 (\code{c(x0, y0)}) to shift the origin.
Overrides \code{r} if both are provided.}

\item{deg}{Logical, whether the angles are in degrees (default = TRUE). If FALSE, assumed in radians.}
}
\value{
A \code{data.frame} with two columns: \code{x} and \code{y}.
}
\description{
This function transforms azimuth and distance values into Cartesian coordinates (x, y).
Useful for forestry, surveying, or spatial applications where polar measurements are common.
}
\examples{
# Example with degrees
coord_xy(azi = c(0, 90, 180), dis = c(10, 10, 10))

# Example with radians
coord_xy(azi = c(0, pi/2, pi), dis = c(10, 10, 10), deg = FALSE)

# Using center
coord_xy(azi = c(0, 90), dis = c(5, 5), center = c(2, 2))

}
